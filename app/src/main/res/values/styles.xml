<resources>
    <style name="Widget.Theme.SouffleTeamPrototype.ActionBar.Fullscreen" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

    <style name="Widget.Theme.SouffleTeamPrototype.ButtonBar.Fullscreen" parent="">
        <item name="android:background">@color/black_overlay</item>
        <item name="android:buttonBarStyle">?android:attr/buttonBarStyle</item>
    </style>

    <!-- Update the RoundedButton style to set background to @color/Peach -->
    <style name="RoundedButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/Peach</item> <!-- Change to Peach color -->
        <item name="android:textColor">#333</item>
    </style>
</resources>
